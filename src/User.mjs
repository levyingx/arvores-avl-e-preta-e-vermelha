export default class User {
    constructor(login, userid, region, password) {
        this.login = login;
        this.userid = userid;
        this.region = region;
        this.password = password;
    }
}